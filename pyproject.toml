[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "yubtc"
version = "0.1.0"
description = "Console BTC client that stores nothing on disk; private keys are derived from a seed."
readme = "README"
requires-python = ">=3.9"
license = { file = "LICENSE" }
authors = [{ name = "Yuri Dyachenko", email = "pub.yurial@gmail.com" }]
dependencies = [
    "requests",
    "click",
    "ecdsa",
    "base58",
    "safe-pysha3",
]

[project.optional-dependencies]
test = ["pytest>=7.0", "pytest-cov"]

[project.scripts]
yubtc = "yubtc.cli:cli"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.coverage.run]
source = ["yubtc"]
branch = true

[tool.coverage.report]
show_missing = true
# Raised alongside observed coverage. The 0.5pp buffer keeps the gate passing
# while still catching bulk regressions. Hold-down files: net.py (untouched),
# wallet.py, cli.py, script.py.
fail_under = 65
